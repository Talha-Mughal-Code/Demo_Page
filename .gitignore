node_modules/
.env
.env.local
.DS_Store
npm-debug.log*

# Runtime data store — images + evaluation results
data/results.json
data/uploads/

# data/catalog/ is deliberately NOT ignored. It is generated by
# `npm run import:catalog`, but a deployment has no way to run that against a
# local .xlsx — the catalog has to ship with the code or the deployed app has no
# products to identify.
